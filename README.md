# ⚙️ core

Personal automation payload. Sealed — nothing readable here without the key.

- **Run:** Actions → ⚙️ core → Run workflow (mode: `run`).
- **Stop everyone:** create a file named `STOP` at the repo root — all running
  and future runs halt within ~60 seconds. Delete `STOP` to resume.
